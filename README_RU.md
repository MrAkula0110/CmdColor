# 🎨 CmdColor

Легковесная header-only C++20 библиотека для цветного вывода в терминал с поддержкой анимаций, градиентов и радужных эффектов.

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![C++20](https://img.shields.io/badge/C%2B%2B-20-blue.svg)](https://en.cppreference.com/w/cpp/20)
[![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20Linux-lightgrey.svg)](https://github.com/MrAkula0110/CmdColor)

[English](README.md) | **Русский**

---

## 📋 Содержание

- [Особенности](#-особенности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Быстрый старт](#-быстрый-старт)
- [Документация API](#-документация-api)
    - [Базовая раскраска](#базовая-раскраска)
    - [Радужный текст](#радужный-текст)
    - [Градиенты](#градиенты)
    - [Цветные диапазоны](#цветные-диапазоны)
    - [Анимации](#анимации)
    - [Вспомогательные функции](#вспомогательные-функции)
- [Примеры использования](#-примеры-использования)
- [Поддержка платформ](#-поддержка-платформ)
- [Лицензия](#-лицензия)
- [Поддержать проект](#-поддержать-проект)
- [Автор](#-автор)

---

## ✨ Особенности

- 🎨 **True Color поддержка** — 16.7 миллионов цветов (RGB)
- 🌈 **Радужные эффекты** — автоматическая раскраска текста в цвета радуги
- 🎭 **Градиенты** — плавные переходы между двумя цветами
- 🎬 **Анимации** — живые радужные и градиентные эффекты
- 🎯 **Точечная раскраска** — выделение конкретных участков текста
- 📦 **Header-only** — просто скопируйте один файл!
- ⚡ **Без зависимостей** — только стандартная библиотека C++20
- 🖥️ **Кросс-платформенность** — Windows и Linux/macOS
- 🔧 **UTF-8 поддержка** — корректная работа с кириллицей и эмодзи

---

## 🔧 Требования

- **Компилятор:** GCC 10+, Clang 11+, MSVC 2019+ (с поддержкой C++20)
- **Стандарт:** C++20 или выше
- **Платформа:** Windows 10+, Linux, macOS

### Проверка поддержки C++20:

```bash
# GCC
g++ --version  # Должна быть версия 10 или выше

# Clang
clang++ --version  # Должна быть версия 11 или выше

# MSVC
cl  # Visual Studio 2019 или новее
```

---

## 📦 Установка

### Способ 1: Копирование файла (рекомендуется)

Просто скопируйте `CmdColor.hpp` в ваш проект:

```bash
# Скачайте файл
curl -O https://raw.githubusercontent.com/MrAkula0110/CmdColor/include/CmdColor.hpp

# Или клонируйте репозиторий
git clone https://github.com/MrAkula0110/CmdColor.git
```

### Способ 2: CMake подмодуль

```bash
# Добавьте как git submodule
git submodule add https://github.com/MrAkula0110/CmdColor.git external/CmdColor
```

В вашем `CMakeLists.txt`:

```cmake
include_directories(external/CmdColor/include)
```

### Способ 3: Прямое включение

Просто поместите `CmdColor.hpp` рядом с вашим исходником и подключите:

```cpp
#include "CmdColor.hpp"
```

---

## 🚀 Быстрый старт

### Минимальный пример

```cpp
#include "CmdColor.hpp"
#include <iostream>

int main() {
    using namespace cmd_color;
    
    // Настройка терминала (автоматическая очистка при выходе)
    TerminalSetup setup;
    
    // Простой цветной текст
    std::cout << colorize("Красный текст!", Colors::RED) << "\n";
    
    // Радуга
    std::cout << rainbow("Радужный текст!") << "\n";
    
    // Градиент
    std::cout << gradient("Градиент от синего к зелёному", 
                          Colors::BLUE, Colors::GREEN) << "\n";
    
    return 0;
}
```

### Компиляция

```bash
# GCC/Clang
g++ -std=c++20 main.cpp -o myapp

# MSVC
cl /std:c++20 main.cpp

# CMake
cmake -B build -DCMAKE_CXX_STANDARD=20
cmake --build build
```

---

## 📚 Документация API

### Базовая раскраска

#### `colorize(text, color)`
Раскрашивает весь текст одним цветом.

```cpp
std::cout << colorize("Зелёный текст", Colors::GREEN) << "\n";
std::cout << colorize("Синий текст", Colors::BLUE) << "\n";
```

#### `colorize_bg(text, fg_color, bg_color)`
Раскрашивает текст с цветом фона.

```cpp
std::cout << colorize_bg("Чёрный на белом", 
                         Colors::BLACK, 
                         Colors::WHITE) << "\n";
```

#### Доступные цвета

Библиотека предоставляет готовые цвета в пространстве имён `Colors::`:

```cpp
Colors::RED        // Красный
Colors::GREEN      // Зелёный
Colors::BLUE       // Синий
Colors::YELLOW     // Жёлтый
Colors::CYAN       // Голубой
Colors::MAGENTA    // Пурпурный
Colors::WHITE      // Белый
Colors::BLACK      // Чёрный
Colors::ORANGE     // Оранжевый
Colors::PURPLE     // Фиолетовый
```

#### Пользовательские цвета

Создайте свой цвет с помощью структуры `Color`:

```cpp
Color custom_color{128, 0, 255};  // RGB: фиолетовый
std::cout << colorize("Мой цвет", custom_color) << "\n";
```

---

### Радужный текст

#### `rainbow(text, start_phase = 0.0)`
Раскрашивает каждый символ в цвета радуги.

```cpp
std::cout << rainbow("Это радужный текст!") << "\n";

// С пользовательской начальной фазой
std::cout << rainbow("Смещённая радуга", 1.5) << "\n";
```

#### `rainbow_range(text, start, end)`
Раскрашивает радугой только указанный диапазон символов.

```cpp
// "Hello" будет радужным, остальное - обычным
std::cout << rainbow_range("Hello World!", 0, 5) << "\n";
```

---

### Градиенты

#### `gradient(text, from_color, to_color)`
Создаёт плавный переход между двумя цветами.

```cpp
// От красного к синему
std::cout << gradient("Красивый градиент", 
                      Colors::RED, 
                      Colors::BLUE) << "\n";

// От жёлтого к пурпурному
std::cout << gradient("Закатный градиент", 
                      Colors::YELLOW, 
                      Colors::MAGENTA) << "\n";

// Пользовательские цвета
Color dark_blue{0, 0, 139};
Color light_blue{173, 216, 230};
std::cout << gradient("Морской градиент", dark_blue, light_blue) << "\n";
```

---

### Цветные диапазоны

#### `colorize_ranges(text, ranges)`
Раскрашивает конкретные части текста разными цветами.

```cpp
ColorRange ranges[] = {
    {0, 5, Colors::RED},      // Символы 0-4: красный
    {6, 11, Colors::GREEN},   // Символы 6-10: зелёный
    {12, 17, Colors::BLUE}    // Символы 12-16: синий
};

std::cout << colorize_ranges("Hello World !!!", ranges) << "\n";
```

**Результат:** `Hello` (красный) `World` (зелёный) `!!!` (синий)

---

### Анимации

⚠️ **Важно:** Анимации запускаются в отдельном потоке и блокируют экран. Запускайте их в конце программы или в отдельном потоке.

#### `animate_rainbow(stop_token, text, interval)`
Анимированная радуга, цвета плавно сменяют друг друга.

```cpp
#include <thread>

// Запуск анимации на 5 секунд
std::jthread thread(animate_rainbow, "Анимация!", 30ms);
std::this_thread::sleep_for(std::chrono::seconds(5));
thread.request_stop();  // Остановка анимации
```

**Параметры:**
- `stop_token` — токен для остановки (автоматически с `std::jthread`)
- `text` — текст для анимации
- `interval` — интервал обновления (по умолчанию 30ms)

#### `animate_gradient(stop_token, text, color1, color2, interval)`
Анимированный градиент между двумя цветами.

```cpp
std::jthread thread(animate_gradient, 
                    "Градиент!", 
                    Colors::RED, 
                    Colors::BLUE, 
                    50ms);
                    
std::this_thread::sleep_for(std::chrono::seconds(5));
thread.request_stop();
```

---

### Вспомогательные функции

#### `clear_screen()`
Очищает экран терминала.

```cpp
clear_screen();
```

#### `move_cursor(x, y)`
Перемещает курсор в указанную позицию.

```cpp
move_cursor(10, 5);  // Колонка 10, строка 5
std::cout << "Текст в позиции (10, 5)";
```

#### `hide_cursor()` / `show_cursor()`
Скрывает или показывает курсор.

```cpp
hide_cursor();
// Ваш код...
show_cursor();
```

#### `reset_color()`
Сбрасывает все цвета к стандартным.

```cpp
std::cout << colorize("Цветной", Colors::RED);
reset_color();
std::cout << " Обычный текст";
```

---

## 💡 Примеры использования

### Пример 1: Логирование с цветами

```cpp
#include "CmdColor.hpp"
#include <iostream>

void log_info(const std::string& msg) {
    std::cout << colorize("[INFO] ", Colors::CYAN) << msg << "\n";
}

void log_warning(const std::string& msg) {
    std::cout << colorize("[WARN] ", Colors::YELLOW) << msg << "\n";
}

void log_error(const std::string& msg) {
    std::cout << colorize("[ERROR] ", Colors::RED) << msg << "\n";
}

int main() {
    using namespace cmd_color;
    TerminalSetup setup;
    
    log_info("Программа запущена");
    log_warning("Низкий уровень памяти");
    log_error("Не удалось открыть файл");
    
    return 0;
}
```

### Пример 2: Цветное меню

```cpp
#include "CmdColor.hpp"
#include <iostream>

void show_menu() {
    using namespace cmd_color;
    
    clear_screen();
    
    std::cout << rainbow("=== ГЛАВНОЕ МЕНЮ ===") << "\n\n";
    
    std::cout << colorize("1.", Colors::CYAN) << " Начать игру\n";
    std::cout << colorize("2.", Colors::GREEN) << " Настройки\n";
    std::cout << colorize("3.", Colors::YELLOW) << " О программе\n";
    std::cout << colorize("4.", Colors::RED) << " Выход\n\n";
    
    std::cout << gradient("Выберите опцию: ", Colors::BLUE, Colors::PURPLE);
}

int main() {
    using namespace cmd_color;
    TerminalSetup setup;
    
    show_menu();
    
    int choice;
    std::cin >> choice;
    
    return 0;
}
```

### Пример 3: Прогресс-бар

```cpp
#include "CmdColor.hpp"
#include <iostream>
#include <thread>
#include <chrono>

void show_progress(int percent) {
    using namespace cmd_color;
    
    int bar_width = 50;
    int filled = (bar_width * percent) / 100;
    
    std::cout << "\r[";
    
    // Заполненная часть (зелёная)
    for (int i = 0; i < filled; ++i) {
        std::cout << colorize("█", Colors::GREEN);
    }
    
    // Пустая часть (серая)
    Color gray{100, 100, 100};
    for (int i = filled; i < bar_width; ++i) {
        std::cout << colorize("░", gray);
    }
    
    std::cout << "] " << percent << "%";
    std::cout << std::flush;
}

int main() {
    using namespace cmd_color;
    TerminalSetup setup;
    
    for (int i = 0; i <= 100; ++i) {
        show_progress(i);
        std::this_thread::sleep_for(std::chrono::milliseconds(50));
    }
    
    std::cout << "\n" << colorize("Готово!", Colors::GREEN) << "\n";
    
    return 0;
}
```

### Пример 4: ASCII-арт с цветами

```cpp
#include "CmdColor.hpp"
#include <iostream>

int main() {
    using namespace cmd_color;
    TerminalSetup setup;
    
    std::cout << gradient("    ____            __  ", Colors::CYAN, Colors::BLUE) << "\n";
    std::cout << gradient("   / __ \\__  ____  / /_ ", Colors::CYAN, Colors::BLUE) << "\n";
    std::cout << gradient("  / /_/ / / / / / / / / ", Colors::CYAN, Colors::BLUE) << "\n";
    std::cout << gradient(" / _, _/ /_/ / /_/ / /  ", Colors::BLUE, Colors::PURPLE) << "\n";
    std::cout << gradient("/_/ |_|\\__,_/\\__,_/_/   ", Colors::BLUE, Colors::PURPLE) << "\n\n";
    
    std::cout << rainbow("Welcome to CmdColor!") << "\n";
    
    return 0;
}
```

---

## 🖥️ Поддержка платформ

### Windows

- ✅ Windows 10 и выше (с включённой поддержкой ANSI)
- ✅ Windows Terminal (рекомендуется)
- ✅ PowerShell
- ✅ cmd.exe (с ограничениями)

**Примечание:** Библиотека автоматически включает поддержку ANSI через `ENABLE_VIRTUAL_TERMINAL_PROCESSING`.

### Linux / macOS

- ✅ Все современные терминалы
- ✅ GNOME Terminal
- ✅ Konsole
- ✅ iTerm2
- ✅ Терминал macOS

---

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).

```
MIT License

Copyright (c) 2025 MrAkula

Данная лицензия разрешает бесплатное использование, копирование, изменение 
и распространение программного обеспечения при условии сохранения 
уведомления об авторских правах.
```

---

## 💖 Поддержать проект

Если вам понравилась библиотека CmdColor, вы можете поддержать разработку:

**[☕ Поддержать на DonationAlerts](https://www.donationalerts.com/r/mrakula228)**

Ваша поддержка помогает развивать проект и добавлять новые функции! 🙏

---

## 👤 Автор

**MrAkula** (Даниэль)

- GitHub: [@MrAkula0110](https://github.com/MrAkula0110)
- DonationAlerts: [mrakula228](https://www.donationalerts.com/r/mrakula228)

---

## 🤝 Вклад в проект

Приветствуются любые предложения и улучшения!

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

## 📝 Changelog

### v1.0.0 (2025-01-01)
- ✨ Первый релиз
- 🎨 True Color поддержка
- 🌈 Радужные эффекты
- 🎭 Градиенты
- 🎬 Анимации
- 🖥️ Поддержка Windows и Linux

---

## ⭐ Поставьте звезду!

Если проект вам полезен, поставьте ⭐ на GitHub — это очень мотивирует!


**Спасибо за использование CmdColor!** 🎉
